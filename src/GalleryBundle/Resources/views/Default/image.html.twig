{% extends "BlogBundle::Master/template.html.twig" %}

{% block title %}Index{% endblock %}

{% block head %}
    {{ parent() }}
    <style type="text/css">
        .important { color: #336699; }
    </style>
{% endblock %}

{% block content %}

    <h1>{{ picture.name }}</h1>
    <img src="{{ picture.filename }}" alt="{{ picture.name }}" />
    <p> Description: {{ picture.description }}</p>
 
{% endblock %}

